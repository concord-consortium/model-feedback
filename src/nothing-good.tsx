    // const textbox:any = context.div;
    // const config = context.config;
    // const jquery = (window as any).$;
    // const reactDiv = jquery('<div id="reactTarget">');

    // textbox.parent().append(reactDiv);
    // const onComplete = () => null;

    // const configView = React.createElement(ConfigurationView, {value: value, onComplete: onComplete}, null);
    // // (jqDiv as Element).appendChild(reactContainer);
    // ReactDOM.render(configView, reactDiv[0]);